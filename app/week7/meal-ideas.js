import react, {useEffect, useState } from 'react';


export default function mealList(){

    //setting up the state variable as meals as an empty array
    //and the set meals being used to update the array
    const [meals, setMeals] = useState([]);

    return(
        <main>
            
        </main>
    )
}







